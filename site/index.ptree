#lang pollen

home.html
teste.html
◊archive.html{
  ◊(for/splice [(file (all-posts))]
       (path->string file))
}

◊(require racket/string)
◊(define tags->posts (gather-tags))
◊(make-tag-pages tags->posts)

◊tags.html{
  ◊(for/splice ([tag->posts (hash->list tags->posts)])
     (path->string (build-path "tags"
                     (string-append* (list (car tag->posts) ".html")))))
}
